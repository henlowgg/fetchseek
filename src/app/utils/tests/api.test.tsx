

// it('should send a POST request to the login endpoint with the provided name and email, and return the response data', async () => {
//     const api = new Api();
//     const name = 'John';
//     const email = 'john@example.com';
//     const expectedResponse = { success: true };

//     const response = await api.loginReq(name, email);

//     expect(response).toEqual(expectedResponse);
//   });